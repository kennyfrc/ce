{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "noEmit": true,
    "rootDir": "."
  },
  "include": ["src/**/*", "test/**/*", "test/global.d.ts"],
  "exclude": [
    "node_modules",
    "dist",
    "**/*.tmp",
    "**/*.backup*",
    "**/*.orig",
    "**/*.ts.bak"
  ]
}
